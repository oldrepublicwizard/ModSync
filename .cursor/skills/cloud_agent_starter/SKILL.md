# Deprecated — use cloud-agents-starter

This skill is **deprecated**. Use:

**`.cursor/skills/cloud-agents-starter/SKILL.md`**

The deprecated file used an incorrect test project path (`ModSync.Tests/` without `src/`). All cloud agent guidance was merged into `cloud-agents-starter`, including security notes and knowledgebase links.
