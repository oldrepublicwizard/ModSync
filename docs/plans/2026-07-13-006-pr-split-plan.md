# PR split plan — FOMOD gate (#170) vs paste / vision / release / protocol

Date: 2026-07-13  
Status: Active  
Goal: Keep [PR #170](https://github.com/th3w1zard1/ModSync/pull/170) FOMOD-only; park mixed WIP on clearly named branches without destructive history rewrite or force-push.

## Snapshot (local, no push)

| Branch | Tip (abbrev) | Role |
|--------|--------------|------|
| `origin/feat/fomod-configuration-gate` / local `feat/fomod-configuration-gate` | `b09a4e7e` | **PR #170 only** — matches remote |
| `feat/guide-paste-ingestion` | `653c7771` | Next PR: paste + page-0 + headless smoke (stacked on FOMOD) |
| `docs/release-gap-inventory` | `1bbc14e5` | Release gap inventory + readiness checklist docs |
| `feat/modsync-protocol-handler` | `1bbc14e5` | Protocol Phase 1 WIP base (same tip as release docs for now) |
| `feat/guide-ingestion-quality` | beyond `1bbc14e5` | Integration / quality follow-ups; **do not push onto #170** |

Stashes (recover later; do not drop): `wip: mixed FOMOD/paste/protocol/docs before PR split`, `wip: concurrent agent dirt during PR split`, `wip: force-clear for PR split`, plus older protocol stashes.

## Commit classification

### PR #170 — FOMOD configuration gate (keep)

Already on `origin/feat/fomod-configuration-gate` / open PR:

1. `7b27a582` — feat: block validate/install until FOMOD archives are configured  
2. `2c636c75` — fix(review): close FOMOD gate bypasses on single-install and missing mod dir  
3. `b09a4e7e` — fix(fomod): fail closed when downloaded archives cannot be inspected  

**In scope for #170:** `FomodConfigurationGate`, validate/install fail-closed paths, probe/enumerate fail-closed, review fixes, FOMOD-only tests/docs that explain the gate.

**Out of scope for #170:** paste/guide ingestion, product STRATEGY/vision, page-0 layout, headless paste smoke, release inventory, `modsync://` protocol.

Optional #170 follow-ups (only if still needed after review; cherry-pick onto `b09a4e7e`, do not attach paste stack):

- Headless `InstallStartPage` FOMOD gate smoke (`FomodGateHeadlessTests` — currently in stash)  
- Configure-FOMOD recovery merge into selected mod (`ModManagementDialog` / merger) if not already on remote  
- FOMOD KB shipped-status wording that does not pull in paste/protocol indexes  

### Next PR — guide paste ingestion (`feat/guide-paste-ingestion`)

Commits **after** FOMOD tip `b09a4e7e` through `653c7771`:

1. `625cb41b` — docs: add product strategy and guide-ingestion vision  
2. `a37efbaf` — feat: paste guide text into ModSync with draft instructions  
3. `0efc1724` — fix(gui): make log viewers resizable and tighten page-0 layout  
4. `653c7771` — test(gui): add Avalonia headless UX smoke for paste and page-0  

**Base strategy:** Open stacked on #170 **or** rebase onto `master` after #170 merges. Do **not** push these four commits to `feat/fomod-configuration-gate`.

### Docs PR — release gap inventory (`docs/release-gap-inventory`)

Commits after paste tip:

1. `b813683e` — docs: inventory release gaps for ModSync (`2026-07-13-002-release-gap-inventory.md`)  
2. `1bbc14e5` — docs: add release readiness checklist and sync shipped state (`005-release-readiness-checklist.md`)  

Can land independently of paste code if rebased onto `master` (docs-only), or stay stacked until paste lands.

### Later — `modsync://` protocol (`feat/modsync-protocol-handler`)

Phase 1 code/docs currently parked in stashes / partial commits on integration branches:

- `ModSyncUrl`, `ModSyncHandoffQueue`, CLI `--modsync` / handoff wiring, tests  
- Brainstorm + KB + OS-registration plan  

**Preferred:** After #170 (and preferably paste) merge, branch from `master` and apply protocol commits/stash slices only — avoid opening a PR that still contains the FOMOD+paste stack.

### Integration only — `feat/guide-ingestion-quality`

Holds mixed follow-on commits (shipped-brainstorm closure, STRATEGY stubs, KB cross-links, wizard AXAML polish). Use for local integration; **split before any PR**. Not a merge candidate as-is.

## What was done locally (non-destructive)

1. Inventoried `origin/master..` stacks; confirmed remote #170 is already clean (3 commits).  
2. Local `feat/fomod-configuration-gate` had been **ahead 4** with paste/vision commits — **repointed** with `git branch -f` to `origin/feat/fomod-configuration-gate` so accidental push cannot pollute #170. Those four commits remain on `feat/guide-paste-ingestion`.  
3. Created/updated: `feat/guide-paste-ingestion` @ `653c7771`, `docs/release-gap-inventory` @ `1bbc14e5`.  
4. Stashed mixed dirty WIP (no `reset --hard`, no force-push, no dropped commits).  

## Do / don’t

| Do | Don’t |
|----|--------|
| Keep #170 at remote tip `b09a4e7e` | Force-push `feat/fomod-configuration-gate` with paste commits |
| Open paste PR from `feat/guide-paste-ingestion` | Close or retarget #170 to a mixed branch |
| Recover protocol/FOMOD test WIP from stashes onto the right tip | `reset --hard` / drop commits |
| Renumber plan files if needed (see below) | Assume local `feat/guide-ingestion-quality` is PR-ready |

## Plan file numbering note

Collision: `docs/plans/2026-07-13-002-release-gap-inventory.md` (release) vs proposed `2026-07-13-002-feat-modsync-protocol-os-registration-plan.md` (protocol). Prefer renumbering the protocol OS-registration plan to **`007`** (or next free) when committing protocol docs so `002` stays release-gap inventory.

## Recommended PR contents

### #170 (ship now)

- Only the three FOMOD gate commits above.  
- No paste, vision STRATEGY, page-0, release inventory, or `modsync://`.  

### Next PR (paste)

- Branch: `feat/guide-paste-ingestion`  
- Commits: `625cb41b` … `653c7771`  
- Title sketch: paste guide text → draft instructions + page-0/headless UX  

### Following docs PR (optional)

- Branch: `docs/release-gap-inventory`  
- Commits: `b813683e`, `1bbc14e5` (+ this plan `006`)  

### Protocol PR (later)

- Fresh tip from `master` after dependencies land; apply stash/protocol files only.  

## gh strategy note

Comment on #170 pointing reviewers at this plan: remote head is already correct; local mixed commits were moved to `feat/guide-paste-ingestion` / docs / protocol branches. Do not close other PRs as part of this scrub.
